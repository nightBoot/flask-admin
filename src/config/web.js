import app from 'assets/images/app.png';

export const logo = app;

const web = {
    basename: '/flask-admin',
    apiPrefix: '/api/mui-admin',
    headerHeight: 64,
    drawerWidth: 240,
    menuIconColor: '#B8BABF'
};

export default web;